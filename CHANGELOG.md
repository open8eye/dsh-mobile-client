# 更新日志

这个文件记录每个版本改了什么。**发布时请把对应段落复制进 tag 注释**——App 的「更新说明」显示的正是 tag 注释。

版本号规则与发布流程见 [docs/RELEASING.md](docs/RELEASING.md)。

## [1.1.6] - 2026-09-21

### 修复

- **点「更新」被要求先卸载，卸载完安装包也没了**。安装包下载在 App 的私有缓存里，
  而卸载会把整个目录删掉——提示让你卸载，卸载又毁掉唯一那份包，只能回网页重新下载。
  现在交给系统安装器**之前**先比对签名：不同就不启动安装器，改为把安装包复制一份到
  系统「下载」目录，并直接告诉你去那里装（Android 10 以下没有免权限的公共目录，
  改为提供「分享安装包」按钮）
- **每个版本的签名都不一样**。release 构建一直用 debug 签名，而 CI 每次都在全新的
  runner 上现场生成一把新密钥：v1.1.4 的证书指纹是 `52975f23…`，v1.1.5 是
  `3f984c80…`，本机构建又是 `7a8f9207…`。于是每次更新都必然撞上「签名不同」。现在
  构建读 `app/android/key.properties`（keystore 放在仓库外），CI 缺它就直接失败，
  不会再悄悄发出一个装不上去的包

### 说明

- **这一版仍然要卸载重装一次**：它是第一个用固定密钥签名的版本，和之前所有版本签名
  都不同。从下一版开始，更新就能直接覆盖安装，不必再丢设备列表和访问密码
- 顺带修好一件旧事：Release 的正文以前取到的是**提交信息**而不是 tag 注释
  （`actions/checkout` 把推上来的 tag 落成了轻量 tag），v1.1.2 到 v1.1.5 的
  「更新说明」都带着 `Co-Authored-By` 那行。现在先重新取一次 tag 对象再读注释，
  文档里写的行为终于成立

## [1.1.5] - 2026-09-21

### 修复

- **电脑端重启 DSH 后，手机一直停在 `dsh web authentication required` 这一句**。
  `dsh web` 每次启动都会换掉 launch token 和会话 Cookie 的签名密钥，手机里那张
  `dsh-auth-*` 随即作废；但 dsh-pocket 只在请求**没有**该 cookie 时才补 launch token
  ——它看的是有没有，不是还有没有效。于是握手永远不再发生：刷新、重开 App、
  重输密码，看到的都是同一句 401
- 这种失败以前被 App 提示成「通常是访问密码过期了」，把一个**没坏**的密码指成嫌疑人。
  现在认出这句 401 就自动清掉过期的 Cookie 再连一次（每个页面会话只做一次），
  不再打扰用户
- 触发条件很窄，所以难查：只有「重启前成功连过、重启后没清过数据」的手机才会中招。
  电脑浏览器走的是 dsh web 打印的新地址，从没连过的手机 Cookie 罐是空的，两者都正常

## [1.1.4] - 2026-09-21

### 修复

- **发布流程镜像到 Gitee 时一定会失败**。那一步先把 tag 推到 Gitee，但开发者的
  `./tools/push.sh` 已经把 tag 推过去了，于是 git 报「tag 已存在」并返回非零，
  `set -e` 当场中断整个步骤——后面「建发行版」那几行根本没机会跑。1.1.3 就是这样：
  GitHub 收到了包，Gitee 什么都没有
- 那一步失败时还**一句话都不说**：`curl -f` 把 Gitee 返回的原因（令牌缺 scope、
  仓库名不对、发行版已存在）全都压成「exit code 22」。现在不传 `-f`，把响应体原样打出来

### 说明

- 这一版的 App 内容与 1.1.3 完全相同，改的只有发布流程。发它的目的是让修好的镜像
  真的跑一遍——否则下次发版时这些坑还得再踩一次

## [1.1.3] - 2026-09-21

### 修复

- **两条发布渠道以前指向同一个 owner，而它们其实是两家**：GitHub 上是
  `open8eye/dsh-mobile-client`，Gitee 上是 `lulendi/dsh-mobile-client`。打包时两边
  都用了后者，于是 GitHub 那条路永远 404。这件事是**安静**的——检查更新只把那一侧
  记成「连不上」，然后靠另一侧工作，看起来一切正常，直到能用的那一侧出问题为止
- 发布流程在 `GITEE_REPO` 没配置时会拿 `github.repository` 顶上，于是打出一个 Gitee
  通道指向错误地址的包。现在没配置就不传这个 define，让 App 自己的默认值生效
- CI 上传产物时路径写错了（写 `app-release.apk`，实际是 `app-standard-release.apk`），
  配合 `if-no-files-found: error`，每次 master 推送的 CI 都在最后一步红掉，而
  analyze / test / build 全是绿的——读起来像基础设施抽风，不像路径写错

## [1.1.2] - 2026-09-21

### 修复

- **内置内核版（legacy）从 110.7 MB 降到 69.2 MB**，这才塞得进 Gitee 的「单个附件
  不超过 100 MB」。原因是一个想当然的假设：那个内置的 WebView 内核自己就是个 zip，
  于是打包时被设成「不压缩」。但它内部大部分条目本来就是**不压缩**存放的（原生库、
  dex），所以再压一次仍然省掉 47% —— 88.7 MB → 47.1 MB。代价是首次解包时多一次解压，
  而那是每次安装只做一次、且结果会缓存在磁盘上的动作

## [1.1.1] - 2026-09-21

### 修复

- **还没发布过任何版本时，「检查更新」不再只说 `HTTP 404`**。两个站都没有发行版时，
  界面原本显示 `GitHub: HTTP 404 / Gitee: HTTP 404`——用户看到这个只会一头雾水。
  404 不是网络故障，而是「仓库不存在、没公开，或者还没发布过发行版」，现在直接这么写

## [1.1.0] - 2026-09-21

首个正式发布。此前 `1.0.0`–`1.0.5` 都只是本地构建，没有对外发过 tag，
所以这里把从零到现在的用户可见改动一次说清。

### 界面

- 底部导航栏改成**纯白底、矮一点**（64 → 56）
- **刷新按钮挪到设备名旁边**，并且只在会话页（正在看当前设备）出现。此前它单独占一格，
  在别的页面上按了也不知道刷的是谁。名字和刷新共用一个定宽槽位，所以五个图标不会跟着动
- 导航栏中间的名字：**点一下回到当前设备，长按才是编辑**
- **屏蔽横向滑动返回**：Android 的边缘返回手势 + WebView 自己的过滚动历史回退，两条路都堵上
- 底部导航栏五个纯图标 + 中间设备名：扫一扫 / 运行实例数 / 设备名称 / 设备列表 / 设置
- 重画应用图标：DeepSeek 蓝底上一尾白色的鲸尾

### 诊断

- **登录页那条路以前一条日志都不留**，于是报告里「PIN 被拒了」和「PIN 根本没发出去」
  长得一模一样。现在逐步记：服务端返回了登录页 / 用存好的 PIN 重试一次 / 没有存 PIN 要问用户 /
  存好的 PIN 已经试过了要问用户 / 用户取消了 / 用户输入了几位。只记位数，不记内容
  （而且日志写入时本来就会按 `registerSecret` 抹掉 PIN）

### 修复

- **引导拦截点错了按钮**。此前按「第一个有文字的按钮」找「跳过全部」，
  但引导界面上第一个有文字的按钮是**语言选择器**（`简体中文`），于是脚本打开了一个语言
  下拉菜单，而清单还停在那里。现在按**结构**找，不按文案：清单页取 `<footer>` 里的按钮，
  总结页取 `<h1>` 所在块里的按钮；语言选择器靠 `aria-haspopup`（弹出菜单触发器）排除，
  图标关闭键靠「没有文字」排除，逐项任务行的「配置 / 跳过」靠作用域排除
- 引导拦截现在**全程上报**：每次看到引导界面就记一行 `onboarding: surface seen inert=…
  buttons=[…]`，点到什么记一行 `onboarding: clicked '…'`。以前它静默工作，出问题只能靠猜

### 连接

- **一台设备可以存两个地址**：主地址之外再加一个「备用地址」（通常是 Tailscale 地址）。
  连接时两个地址**同时探测**，用先回应的那个——在家走局域网，出门走 Tailscale，不用手动切。
  只有一个地址时不做探测，不额外增加延迟
- **添加 / 编辑设备不再要求输入密码**。密码改成在真正连上去、服务器开口要的那一刻才弹出输入，
  输一次即存进系统密钥库（Android Keystore / iOS Keychain）。此前「表单里填一遍、连上去再填一遍」
  是同一件事做两次
- 修复连上后**被问两次密码**：保存密码会重建 WebView，而那次重建自己又会发起一次导航
- **连上后自动点掉 DSH 的首次引导弹窗**。DSH 把设置持久化绑在 loopback 上
  （`remote.$host.isLoopback ? 'host' : 'memory'`），手机的确认只活在一个 JS 变量里、
  刷新即失效——所以在已经配置好的 DSH 上，它每次重载都会再来一遍。与其去动那条安全边界
  （那等于让手机能改电脑的设置文件），不如替用户点掉它自己的两个出口：清单上的「跳过全部」
  和总结页的「开始体验」。设置页可关

### 文档

- **README 说清楚「套壳」而不是「装进手机」**。原文案是「把电脑上的 DeepSeek Harness 装进手机」，
  容易被读成 APK 里打包了一份 DSH。实际上本 App 里没有 DSH：它是用 WebView 打开你电脑上
  已经跑着的那个 DSH 网页，模型调用、代码执行、文件读写、会话记录全部仍然在电脑上

### 版本号

- 版本号 `1.1.0`（`versionCode` 7）

## [1.0.2] - 2026-09-20

### 图标

- 重画应用图标：DeepSeek 蓝底上一尾白色的鲸尾，几何图形生成（`tools/make_icons.py`），
  自适应图标、传统图标和 iOS 图标全部由同一份几何重出
- 自适应图标的前景按 108dp 画布作画，实测最远像素落在安全圆内，任何蒙版都不会切到

### 桌宠

- **桌宠暂缓**，设置页不再显示入口。代码、Android 悬浮窗服务与设置项全部保留，
  开关是 `PetFeature.available`
- 升级上来的用户如果之前开着桌宠，启动时会主动关掉，避免留下一个关不掉的悬浮窗

### 连接

- **一台设备可以存两个地址**：主地址之外再加一个「备用地址」（通常是 Tailscale 地址）。
  连接时两个地址**同时探测**，用先回应的那个——在家走局域网，出门走 Tailscale，不用手动切。
  只有一个地址时不做探测，不额外增加延迟
- 设备列表里存了两个地址的设备会多一个 ⇄ 图标
- **添加 / 编辑设备的表单不再有密码输入框**。密码改成在真正连上去、服务器开口要的那一刻
  才弹出输入，输一次即存进密钥库。此前「表单里填一遍、连上去再填一遍」是同一件事做两次
- 修复连上后**被问两次密码**：保存密码会重建 WebView，而那次重建自己又会发起一次导航，
  与弹窗已经发起的导航相互竞争；两次导航就是两次登录页探测，第二次仍可能看到登录页再问一次
- 表单不再接收密码本身，只接收「是否已保存」——设置页是密钥最容易被截图带走的地方

### 交互

- 底部导航栏中间的名字：**点一下回到当前设备**，**长按才是编辑**。此前点一下直接进配置页，
  阅读时误触就会掉进一张表单；而「回到当前设备」才是这个位置最常见的意图
- **屏蔽横向滑动返回**。Android 的边缘返回手势在全屏 WebView 上很容易在滚动时误触，
  此前会直接把 App 弹掉。现在返回键：在其他页回到当前设备；在会话页只提示一次，
  两秒内再按才退出
- 页面侧同时注入 `overscroll-behavior-x:none`，堵住 WebView 自己把横向过滚动变成历史
  回退的那条路径；并显式关掉 `allowsBackForwardNavigationGestures`（iOS 上它默认是开的）

### 版本号

- 版本号从 `1.0.0` 提到 `1.0.2`（`versionCode` 3）。诊断报告里从此显示 `1.0.2 (3)`；
  此前无论重打多少次都是 `1.0.0 (1)`，装的是哪个包只能靠翻 `compat:` 日志分辨

### 界面

- 底部导航栏改成**六个纯图标**：扫一扫 / 运行实例数 / 设备名称 / 设备列表 / 设置 / 刷新
- 去掉当前设备那一页的顶部标题栏，整屏都留给网页；设备类型标签挪进设备配置页
- **多设备同时在线**：连上的设备各自保活一个会话（上限 4 个，超出淘汰最久没看的），
  切换是秒切——不重新加载、不用重新登录
- 实例数按钮显示当前有几个会话，点击弹出选择面板，可在其中切换或关闭某个会话
- 导航栏中间显示当前设备昵称（定宽，过长省略）

### 老设备

- 新增**内置内核版**（`-legacy` 后缀的 APK）：自带一份 AOSP arm32 Chromium 113，
  系统 WebView 太旧、又装不了新内核的机器也能用。不安装任何东西、不碰系统设置
- 该版本自己解包内核（首次启动约 170 MB 磁盘），而不是每次启动重拷一遍，
  避免这次会话的第一个 WebView 仍按旧内核创建
- 该版本只打包 armeabi-v7a，保证以 32 位进程运行，与 32 位内核匹配
- 应用内更新按文件名区分两个版本，各取所需，绝不交叉
- 补上内置内核缺的运行时 API。Chromium 113 解析得了 bundle，却缺
  `Promise.withResolvers`（要 119）和 `AbortSignal.any`（要 116）——后者正好落在会话控制流的
  第一次读取上，症状是**界面出得来、连接永远建不起来**并不断重试
- 兼容 shim 清单扩大到 20 组，每一项都是扫描 DSH 的 bundle 得出的，不再靠猜
- 新增 `tools/compat_check.mjs`：把原生实现删掉、换成 shim、逐项对比结果，CI 里跑
  （需要 Node 24+）。`Object.hasOwn` 等早先就有的 shim 也一并纳入对比

## [1.0.0] - 首个版本

### 连接

- 扫描电脑上「手机访问」页面的二维码添加设备，支持多台 DSH 并存
- 手动填写地址；扫描带 `?token=` 的分享链接时连密码一起保存
- **自动重新登录**：密码存进系统密钥库，每次从 `/?token=` 进入，
  电脑上 `dsh web` 重启后也**不需要重新输入密码**
- 地址识别与 dsh-pocket 对齐：局域网 / Tailscale（CGNAT 100.64/10）/ 公网隧道

### 界面

- 四个图标的底部导航栏：扫一扫 / 当前设备 / 设备列表 / 设置
- 设备可自定义昵称，昵称直接显示在导航栏上
- 中英文双语，跟随系统

### 通知

- 网页通知转发为手机系统通知，可选择「仅后台时通知」
- 同一会话的通知按 tag 替换而不是堆叠

### 桌面伙伴（Android）

- 动漫角色悬浮在桌面，可拖动、点按回到 App
- 内置矢量角色，也可换成自己的图片
- iOS 系统不允许第三方 App 覆盖桌面，因此只有应用内预览

### 更新

- 应用内检查 GitHub / Gitee 上的新版本，直接下载并调起系统安装器
- 启动时静默检查一次，可关闭

### 隐私

- 不收集、不上传任何数据；没有账号体系，没有统计埋点
- 访问密码只存在系统密钥库，密钥库写入失败时宁可报错也不回退明文
