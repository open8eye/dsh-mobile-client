<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Reach the DSH server: on the LAN, over Tailscale, or through a tunnel. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Scan the dsh-pocket QR code. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Mirror page notifications into system notifications. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Float the companion over the launcher. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Hand a downloaded update to the system package installer. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
          The floating companion. A window of type TYPE_APPLICATION_OVERLAY may
          only be added by a foreground service, and `specialUse` is the correct
          type for a user-requested decorative overlay.
        -->
        <service
            android:name=".PetOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating desktop companion explicitly enabled by the user" />
        </service>

        <!--
          The updater passes a downloaded APK to the system installer as a
          content URI; a file:// URI is rejected on Android 7+.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
          Swaps in a newer WebView kernel before anything can create a WebView.
          A ContentProvider's onCreate fires during bindApplication — earlier
          than Application.onCreate — and that is the only window in which the
          provider can still be changed. Stores nothing, exposes nothing.
        -->
        <provider
            android:name=".WebViewKernelProvider"
            android:authorities="${applicationId}.webview-kernel"
            android:exported="false"
            android:initOrder="100" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Opening DSH links in a real browser. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>

        <!--
          WebView kernels we may switch to. Android 11+ hides unlisted packages
          from us, and an invisible kernel is an unusable one.
        -->
        <package android:name="com.google.android.webview" />
        <package android:name="com.google.android.webview.beta" />
        <package android:name="com.google.android.webview.dev" />
        <package android:name="com.google.android.webview.canary" />
        <package android:name="com.android.chrome" />
    </queries>
</manifest>
